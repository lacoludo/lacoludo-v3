function showLines() {

  var line = new ProgressBar.Line("#html", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.9);

  var line = new ProgressBar.Line("#css", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.9);

  var line = new ProgressBar.Line("#javascript", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.5);

  var line = new ProgressBar.Line("#php", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.5);

  var line = new ProgressBar.Line("#mysql", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.5);

  var line = new ProgressBar.Line("#mongodb", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.1);

  var line = new ProgressBar.Line("#express", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.3);

  var line = new ProgressBar.Line("#angular", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.6);

  var line = new ProgressBar.Line("#nodejs", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.4);

  var line = new ProgressBar.Line("#bootstrap", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(1.0);

  var line = new ProgressBar.Line("#foundation", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.7);

  var line = new ProgressBar.Line("#jquery", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.7);

  // var line = new ProgressBar.Line("#react", {
  //   strokeWidth: 4,
  //   easing: 'easeInOut',
  //   duration: 5000,
  //   color: '#01d5da',
  //   trailColor: '#eee',
  //   trailWidth: 4,
  //   svgStyle: {
  //     width: '100%',
  //     height: '100%'
  //   }
  // });
  // line.animate(0.1);

  var line = new ProgressBar.Line("#symfony", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.3);

  var line = new ProgressBar.Line("#sass", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.9);

  // var line = new ProgressBar.Line("#typescript", {
  //   strokeWidth: 4,
  //   easing: 'easeInOut',
  //   duration: 5000,
  //   color: '#01d5da',
  //   trailColor: '#eee',
  //   trailWidth: 4,
  //   svgStyle: {
  //     width: '100%',
  //     height: '100%'
  //   }
  // });
  // line.animate(0.1);

  var line = new ProgressBar.Line("#wordpress", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.8);

  var line = new ProgressBar.Line("#drupal", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.2);

  var line = new ProgressBar.Line("#photoshop", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.9);

  var line = new ProgressBar.Line("#illustrator", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.3);

  var line = new ProgressBar.Line("#optimization", {
    strokeWidth: 4,
    easing: 'easeInOut',
    duration: 5000,
    color: '#01d5da',
    trailColor: '#eee',
    trailWidth: 4,
    svgStyle: {
      width: '100%',
      height: '100%'
    }
  });
  line.animate(0.7);

};
